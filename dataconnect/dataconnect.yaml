specVersion: "v1beta"
serviceId: "cosmos"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cosmos-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
